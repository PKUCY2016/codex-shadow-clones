# 更新记录

## v0.4.2 — 2026-09-24

### 新增

- 面板在每周额度下显示官方 `rateLimitResetCredits.availableCount`；服务端未提供时显示“未知”，不会自动兑换。

### 修复

- 分身同步结束后清理旧版完整历史数据库和重复的逐条聊天备份，按资源保留最新一份。旧版数字命名、异常和不完整目录保留供检查。
- 真实备份清理和随后一次自动同步验收确认：可识别旧版备份不再堆积；保留的三组最新数据库备份通过只读 SQLite 完整性检查。

## v0.4.1 — 2026-09-23

### 修复

- 面板点击 Codex 1 时使用 macOS 的官方应用打开/聚焦路径，不再尝试用独立参数复制原实例数据库。
- 启动器普通启动会检测管理服务版本，发现旧版 0.3.0 时安全重载后再打开面板。

## v0.4.0 — 2026-09-22

### 改进

- 新建分身使用 `~/.codex-shadow/<编号>` 短物理目录，降低 macOS 默认 daemon socket 超长风险；旧 `.runtime/clones` 分身继续兼容。
- 配置同步复制本地 Markdown/JSONL 记忆快照，并复制自动化定义为暂停模板，避免多账号重复执行。
- 增加新模板、暂停自动化、短路径与旧分身删除兼容的回归测试。

### 边界

账号登录、浏览器 Cookie、第三方插件授权和系统级 Computer Use 权限仍按实例独立；运行中的任务继续留在原窗口。

## v0.3.0 — 2026-09-22

### 新增

- 分身卡片支持确认后删除。只操作已退出的受管分身；完整目录移入本地备份，保留恢复收据，保护原实例与共享项目，分身编号不复用。
- 自动检查 GitHub 公开版本清单，每 6 小时一次；面板与菜单栏显示新版提示和安装说明，并可手动检查。
- `launch_shadow.py --restart` 在编译成功后安全重载管理服务与菜单栏，保留 Codex 分身；创建和同步期间拒绝重启。
- macOS CI 覆盖 Python 3.11、3.14 的离线测试、Swift 编译与应用签名验证。

### 改进

- 菜单栏只显示忍者头像，明确关闭窗口后继续运行；忍者菜单和应用菜单支持 ⌘Q 退出。
- 多个实例统一汇总已结束历史，运行目标延后补齐，分叉保留为独立副本，支持定期汇总。
- 统一版本号来源 `version.json`，README 标注本次更新并补充安装、删除和恢复说明。

### 修复

- 新建历史分支在隔离目录重建官方历史索引，保留谱系，重复同步不产生相同分支。
- 分支迁移使用独立数据库，避免多个来源的工具定义互相覆盖；工具结果中的 JSON null 保留原义。
- 启动器的本地认证请求绕过环境代理，避免本地服务识别失败。

### 升级说明

早期安装没有自动检查能力，需要先按 README 手动升级到本版本。更新不静默覆盖本地代码；删除分身保留本地备份，已同步历史也会保留。账号数据始终排除在 Git 仓库之外。
