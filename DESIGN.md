# Codex Shadow Clones：架构与边界

## 数据流

```text
原生菜单栏 / 浏览器面板
            │ 本地令牌认证
            ▼
管理服务（127.0.0.1:18318）
  ├─ 生命周期：创建、打开、聚焦指定实例
  ├─ 额度监控：官方 app-server → 身份与额度
  └─ 快照导入：项目、配置、已结束历史与侧边栏
            │
     ┌──────┼──────┐
   Codex 1 Codex 2 Codex 3 …
   原实例  独立登录 独立登录
     └──────┴──────┘
          共享项目代码路径
```

## 模块职责

`shadow_clones.py` 管理分身注册、进程识别、后台额度刷新和操作互斥。`shadow_dashboard.html` 提供本地面板，`shadow_menubar.swift` 提供原生菜单入口。菜单栏与面板使用同一服务和状态，不分别管理账号。

`desktop_second.py` 定义安装路径与已核验版本，管理器导入这些常量。实例启动使用桌面应用自身的独立数据目录参数；`shadow_focus.swift` 按进程 ID 聚焦，避免只按应用名称选错窗口。

`shadow_quota.py` 通过安装的官方 Codex app-server 查询账号和额度，不发起模型生成请求。`shadow_seed.py` 复制经过筛选的配置与项目入口。`shadow_history.py` 处理本地历史文件、索引和依赖；`shadow_history_state.py` 合并允许复制的侧边栏状态。

`scripts/preflight.py` 只读检查系统、依赖、精确桌面版本与必要素材。它不替代离线回归测试，也不证明真实登录或历史界面已可用。

## 共享和隔离

每个分身有独立的 `CODEX_HOME`、Electron 数据目录、凭据和历史数据库。创建分身不复制原账号登录，使用者在官方窗口分别授权。原实例默认位于 `~/.codex`；分身数据位于仓库私有 `.runtime`。

项目共享原有路径，代码文件的变化对所有打开该目录的实例立即可见。项目列表和配置则是快照，需要再次同步才能更新。当前桌面同时使用项目数据库和界面项目缓存，导入需要保持两者映射一致。

不会共享正在写入的 SQLite 或会话文件。第三方插件缓存、插件登录、浏览器 Cookie 和远程控制状态不作为完整应用克隆复制。配置中的插件启用项不能保证对应插件已经安装或连接。

## 历史快照

导入在目标分身退出后进行。源数据库采用一致性备份，历史边界根据已结束或中止的回合确定，不将滞后的缓存偏移视为最新完整边界。所需祖先历史和分页依赖分别复制，目标使用独立文件。

重复更新会新增源任务，并刷新目标未独立修改的导入记录；目标已变化时保留目标版本并报告冲突。已有目标的侧边栏归属和排序字段需要保留。操作不覆盖未由快照导入的目标新任务。

这是本地历史兼容层，不是官方稳定迁移 API。源文件缺失、无法识别的依赖或旧版分页结构可导致 partial；保留日志不等于官方界面一定能够显示。源实例应当继续保留，快照不能作为唯一备份。

## 切换语义

手动切换按目标进程打开或聚焦窗口。可选自动切换监控面板最后选中的分身，按可用额度、不同账号、读数新鲜度、阈值和冷却筛选目标。

原任务仍由原进程执行；切换不暂停或迁移任务，不自动发送继续指令，不搬运模型缓存，也不把多个订阅变成共享额度池。

## 本地服务与兼容性

管理服务仅绑定 loopback，使用本地访问令牌和 Host / Origin 检查。运行目录不应进入公开仓库。菜单栏退出不会自动结束后台监控或桌面实例。

独立实例参数及历史结构随桌面版本变化，启动器固定核验已支持版本。升级适配需要重新检查启动、隔离、导入与查询行为，不能只删除版本检查。当前版本和已知限制见 [README](README.md)，参考关系见 [来源说明](docs/REFERENCES.md)。

## 多源历史汇总

`shadow_sync.py` 先在线备份各实例数据库，在私有 hub 冻结已结束的普通主任务日志，再对退出的目标逐任务分发。不启动来源 app-server，不合并账号或配置。相同记录按内容前缀判断相等、快进或分叉，禁止以时间戳确定胜者。分页依赖型记录归档并标记 unsupported。

管理器 operation 锁串行化自身的同步/启动动作，写目标前重检运行状态；正在运行的目标排队，后续监控在退出后重试。显式开启自动汇总后每约5–6分钟执行。无法控制绕过管理器的外部CLI或检查后手动启动竞争。

新分支使用规范 rollout 文件名和独立任务 UUID，在 staging 中通过官方 `migrate-rollouts --apply --thread` 生成索引后再导入目标。来源映射保存在私有 sidecar，避免官方重写日志头丢失扩展字段。此命令不恢复任务、不发起模型请求；凭据不进入 staging。
